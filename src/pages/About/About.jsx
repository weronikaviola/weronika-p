import React from "react";

import BaseScreen from "../../components/BaseScreen/BaseScreen";

const About = (props) => {
  return (
      <BaseScreen>
        about
      </BaseScreen>
  )
};

export default About;